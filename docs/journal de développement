## Journal de développement

### Version 0.2.0 — 24/07/2026

#### Réalisé
- Création de l'architecture de l'administration.
- Mise en place de `AdminShell`.
- Création de `AdminHeader`, `AdminSidebar`, `AdminNavigation` et `AdminContent`.
- Intégration du menu mobile avec `Sheet` (shadcn/ui).
- Séparation complète entre le site public et l'administration grâce aux Route Groups (`app/(public)`).
- Déplacement du `Header` et du `Footer` dans le layout public.

#### À valider
- Navigation mobile sur téléphone réel.

#### Prochaine étape
- Mise en place de l'authentification avec Auth.js.
ANDORRE 360 Studio – État d'avancement

Authentification terminée
Objectif

Finaliser la sécurisation du back-office.

Réalisations
Configuration d'Auth.js avec le provider Credentials.
Authentification par e-mail et mot de passe.
Chiffrement des mots de passe avec bcryptjs.
Création du premier compte administrateur.
Mise en place de la page /login.
Mise en place de la déconnexion.
Protection de toutes les routes /admin.
Vérification du rôle ADMIN directement dans la base de données.
Vérification que le compte est actif.
Décisions techniques
Stratégie de session : jwt.
Aucun callback Auth.js personnalisé afin de conserver une configuration simple et stable.
Contrôle des autorisations effectué dans app/admin/layout.tsx via une requête Prisma.
Résultat

Le back-office est désormais accessible uniquement aux utilisateurs actifs ayant le rôle ADMIN.

Prochaine étape

Finaliser le module Articles avant de poursuivre avec la médiathèque et les paramètres du site.

## Session – Finalisation de l'authentification du back-office

### Objectif
Finaliser la sécurisation complète de l'administration.

### Réalisations
- Auth.js configuré avec le provider Credentials.
- Authentification par e-mail et mot de passe validée.
- Vérification des mots de passe avec bcryptjs.
- Protection de toutes les routes `/admin`.
- Vérification du rôle `ADMIN` directement en base de données.
- Vérification que le compte utilisateur est actif.
- Validation du fonctionnement de la connexion et de la déconnexion.
- Projet stable, sans erreur de lint (hors avertissements Next.js sur les balises `<img>`).

### Décision
L'authentification est considérée comme terminée. Aucune évolution ne sera apportée à cette partie tant qu'un nouveau besoin fonctionnel ne le justifiera pas.

### Prochaine étape
Reprendre le développement du module **Articles**.

# Changelog

## Version 0.8.0 – Gestion des vidéos

### Ajout
- Support des vidéos externes (YouTube, Vimeo, TikTok, Facebook).
- Nouvelle table `ExternalVideo`.
- API `GET` et `POST` pour les vidéos externes.
- Intégration des vidéos externes dans la médiathèque.
- Sélection des vidéos depuis l'éditeur d'articles.
- Affichage des vidéos dans les articles publics.

### Améliorations
- Les articles de type « Vidéo » peuvent être publiés sans image de couverture.
- Affichage adapté des articles sans image.
- Correction de l'erreur Next.js liée à `Image` avec un `src` vide.

27/07/2026 – Fin de session
État du projet
✅ Terminé
CRUD complet des catégories.
Administration des catégories opérationnelle.
Gestion des vidéos externes.
Correction de plusieurs erreurs Next.js liées aux images (SportsPageV2, CulturePageV2).
Problème identifié

Le projet contient de nombreuses pages V2 construites sur le même modèle :

SportsPageV2
CulturePageV2
PolitiquePageV2
EconomiePageV2
ImmobilierPageV2
LifestylePageV2
MontagnePageV2
SociétéPageV2
InternationalPageV2
etc.

Toutes utilisent directement :

<Image src={article.image} ... />

Or certaines images sont stockées sous forme de chaîne vide ("") en base.

Next.js 16 refuse cela et déclenche :

Image is missing required "src"

ou

An empty string ("") was passed to the src attribute.
Première correction

Création de :

components/SafeImage.tsx
import Image, { type ImageProps } from "next/image";

type SafeImageProps = Omit<ImageProps, "src"> & {
  src?: ImageProps["src"] | null;
};

export default function SafeImage({
  src,
  alt,
  ...props
}: SafeImageProps) {
  const safeSrc = typeof src === "string" ? src.trim() : src;

  if (!safeSrc) {
    return null;
  }

  return <Image src={safeSrc} alt={alt} {...props} />;
}
Ce qu'il reste à faire

Ne pas corriger les pages une par une.

La bonne solution consiste à remplacer automatiquement tous les :

import Image from "next/image";

par

import SafeImage from "@/components/SafeImage";

et

<Image

par

<SafeImage

sans modifier la logique métier.

⚠️ Éviter un simple remplacement texte.

Préférer :

un codemod AST (jscodeshift, ts-morph ou équivalent),
ou une refactorisation automatisée de l'IDE.
Deuxième amélioration recommandée

Modifier les fonctions :

getArticles()
getArticlesByCategory()

pour normaliser :

image: article.image || null

afin de ne plus jamais propager des chaînes vides.

Troisième amélioration

Dans Prisma et dans les formulaires d'administration :

ne plus enregistrer

""

mais

NULL

pour une image absente.

Important

Le script :

scripts/replace-next-image.mjs

a été commencé mais n'a volontairement pas été exécuté, afin d'éviter une modification massive du projet sans vérification.

Il est préférable de mettre en place un codemod robuste plutôt qu'un remplacement texte global.

Je pense que ce journal donnera un bon contexte à la prochaine personne qui reprendra le projet, sans qu'elle ait à reconstituer tout ce qui a été fait. Repose-toi bien, il est largement temps.


Contexte du projet : Next.js 16 + React 19 + Prisma 7. Le projet ANDORRE 360 utilise plusieurs pages V2 (SportsPageV2, CulturePageV2, etc.). Le problème restant est la gestion des images : certaines ont image = "", ce qui provoque les erreurs Next.js Image is missing required "src". Un composant SafeImage.tsx existe déjà. L'objectif est maintenant de mettre en place une correction globale (codemod ou autre solution robuste), pas de corriger chaque page manuellement.